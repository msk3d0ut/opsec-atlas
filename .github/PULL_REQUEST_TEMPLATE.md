## What this changes



## Checklist

- [ ] Commands are **byte-exact** (not reformatted); placeholders used where values are engagement-specific (`<TARGET-IP>`, `<USER>`, …)
- [ ] Content edited in `src/content/atlas/*.md` (Markdown + frontmatter) or `src/data/*.ts` (libraries), never generated files
- [ ] `npm test` passes and `npm run build` is clean
- [ ] Follows [CONTRIBUTING.md](../CONTRIBUTING.md)
